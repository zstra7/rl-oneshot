# Asset Attribution

User-supplied assets present in this repository:

- `assets/models/car.glb` — supplied car model.
- `assets/textures/*.png` — supplied stadium texture library.

Attribution/licensing details to be filled in when the user provides them.
No other 3D models or images are used; everything else is generated in code
(see `plan/asset_production_pipeline_module_spec.md`).
